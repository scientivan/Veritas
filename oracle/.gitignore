node_modules/
.env
*.log

# Local model cache (transformers.js downloads here) and the vector corpus DB.
.models/
data/*.db
data/*.db-*
# but DO ship the seeded corpus so a hosted oracle (Railway) has the D baseline.
!data/corpus.db

# macOS
.DS_Store
